{
  "type": "object",
  "properties": {
    "pairs": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "same": { "type": "boolean" },
          "confidence": { "type": "number", "minimum": 0, "maximum": 1 },
          "keep": { "type": "string", "enum": ["a", "b"] },
          "notes": { "type": "string" }
        },
        "required": ["same", "confidence"]
      }
    }
  },
  "required": ["pairs"]
}
